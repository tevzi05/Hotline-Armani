using UnityEngine;
using UnityEngine.Audio;

public class SoundController : MonoBehaviour
{
    [SerializeField] private AudioMixer mainMixer; //миксер лол
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
